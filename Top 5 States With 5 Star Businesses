POSTGRE SQL:

WITH c1 AS
(
SELECT state, COUNT(*) AS cnt
FROM yelp_business
WHERE stars = 5
GROUP BY 1
ORDER BY 2 DESC, 1 ASC
)

SELECT state, cnt
FROM c1
WHERE cnt IN (SELECT cnt
FROM c1
LIMIT 5)


PYTHON:


R:

